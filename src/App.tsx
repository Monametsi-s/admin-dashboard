function App({ table }: { table: string }) {
  console.log(table);
  return (
    <div className="flex items-center justify-center bg-blue-100">
      <h1 className="text-4xl font-bold text-red-500">Tailwind's is working!</h1>
    </div>
  );
}

export default App;